import React from 'react';
import {Text, View} from "native-base";

const SideBar = () => {
    return (
        <View>
        <Text>Hi from side bar</Text>
        </View>
    );
};

export default SideBar;